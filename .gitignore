# Build artefacts
/_build
/cover
/deps
/doc
/.fetch

# Erlang crash dumps
erl_crash.dump
*.ez

# BEAM bytecode (committed only if checked into release packages)
*.beam

# Runtime secrets — never commit these
/config/*.secret.exs

# Escript binaries (built with `mix escript.build` in apps/cli)
/apps/cli/cli

# Editor / tooling
.elixir_ls/
.elixir-tools/
.idea/
*.iml
.vscode/
*.swp
*.swo
*~

# OS artefacts
.DS_Store
Thumbs.db
